export enum Role {
  USER = "user",
  ADMIN = "admin",
  Employee = "employee",
}
export enum UserStatus {
  ACTIVE = "active",
  DISABLE = "disable",
}

export enum SessionTokenAction {
  ForgotPassword,
}
